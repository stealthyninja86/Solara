apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: insight-db
  labels:
    app: insight-db
spec:
  serviceName: insight-db
  replicas: 1
  selector:
    matchLabels:
      app: insight-db
  template:
    metadata:
      labels:
        app: insight-db
    spec:
      containers:
        - name: postgres
          image: pgvector/pgvector:pg16
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: db
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: solara-secrets
                  key: SPRING_DATASOURCE_USERNAME
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: solara-secrets
                  key: SPRING_DATASOURCE_PASSWORD
          volumeMounts:
            - name: insight-db-data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "admin_user", "-d", "db"]
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "admin_user", "-d", "db"]
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 192Mi
            limits:
              cpu: 250m
              memory: 256Mi
      volumes:
        - name: insight-db-data
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: insight-db
  labels:
    app: insight-db
spec:
  selector:
    app: insight-db
  ports:
    - port: 5432
      targetPort: 5432
