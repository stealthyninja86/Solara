# Solara production tier — AWS deployment (ECS Fargate / dedicated host).
# AI is OFF: no Ollama, no OpenAI key, zero LLM/embedding traffic.
app.ai.enabled=false
app.merchant-profile.sync-enabled=false